# 365 Weapons Admin Backend Environment Variables

# Required: OpenAI API Key for embeddings, TTS, and Whisper
OPENAI_API_KEY=sk-your-openai-api-key

# Required: OpenRouter API Key for chat/AI completions
OPENROUTER_API_KEY=sk-or-your-openrouter-api-key

# Required: Auth token for API security (generate a secure random string)
API_AUTH_TOKEN=your-secure-auth-token-here

# Optional: LanceDB storage path (default: ./lancedb_data)
LANCEDB_PATH=./lancedb_data

# Optional: Port (default: 8000, Railway sets this automatically)
PORT=8000
